# ShopTheBarber — Server environment (copy to .env and fill in)
# Do not commit .env; it may contain secrets.
# Full list + world-class roadmap: docs/WORLD_CLASS_ROADMAP_AND_ENV.md

# Required: JWT signing secret (use a long random string in production)
JWT_SECRET=your-jwt-secret-change-in-production

# Optional: SQLite DB path (default: sovereign.sqlite in server dir)
# DATABASE_PATH=./sovereign.sqlite

# Stripe (payments) — test mode: use sk_test_... and pk_test_... from Dashboard → API keys
STRIPE_API_KEY=
STRIPE_PUBLISHABLE_KEY=
STRIPE_WEBHOOK_SECRET=

# Frontend URL (for redirects and emails)
FRONTEND_URL=http://localhost:3000

# Backend URL (for Google OAuth redirect_uri; set in production e.g. https://shopthebarber-api.onrender.com)
# BACKEND_URL=https://your-api.onrender.com

# Resend (transactional email)
RESEND_API_KEY=
EMAIL_FROM=Shop The Barber <onboarding@resend.dev>

# Optional: local AI endpoint (e.g. LM Studio)
# LOCAL_AI_ENDPOINT=http://localhost:1234/v1

# --- Reserve for future / optional features (add in Render when you enable them) ---

# Google Sign-In (OAuth) — from Google Cloud Console → APIs & Services → Credentials
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=

# Facebook Sign-In (OAuth) — from Meta for Developers → App → Settings → Basic
# FACEBOOK_APP_ID=
# FACEBOOK_APP_SECRET=

# Geolocation / Maps (e.g. distance, “near me”) — Google Maps or Mapbox
# GOOGLE_MAPS_API_KEY=
# Or: MAPBOX_ACCESS_TOKEN=

# Calendar (if you add Google Calendar sync) — same Google project as OAuth, enable Calendar API
# Uses same GOOGLE_CLIENT_ID / GOOGLE_CLIENT_SECRET + Calendar API scope

# LinkedIn Sign-In + profile import (barbers, stylists, managers) — https://www.linkedin.com/developers/
# LINKEDIN_CLIENT_ID=
# LINKEDIN_CLIENT_SECRET=

# Optional: Grok/cloud AI (when not using LOCAL_AI_ENDPOINT)
# GROK_API_KEY=

# WhatsApp reminders (e.g. Twilio WhatsApp API or Meta Cloud API)
# Twilio: https://www.twilio.com/console
# TWILIO_ACCOUNT_SID=
# TWILIO_AUTH_TOKEN=
# TWILIO_WHATSAPP_NUMBER=whatsapp:+14155238886

# --- Analytics, Security & User Navigation Tracking ---
# Sentry (backend error tracking) — https://sentry.io
# SENTRY_DSN=
# SENTRY_AUTH_TOKEN=
# SENTRY_ENVIRONMENT=production

# Server-side analytics (e.g. Segment write key; optional)
# ANALYTICS_WRITE_KEY=

# Enable backend logging of navigation events (optional; for custom dashboards)
# NAVIGATION_TRACKING_ENABLED=true

# --- Observability & Performance ---
# LOG_LEVEL=info
# DATADOG_API_KEY=
# NEW_RELIC_LICENSE_KEY=

# --- High-Scale Infrastructure & Storage ---
# AWS_S3_BUCKET_NAME=
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_REGION=us-east-1
# CDN_URL=
# REDIS_URL=redis://localhost:6379

# --- Localization & i18n ---
# DEFAULT_CURRENCY=USD
# SUPPORTED_LOCALES=en,fr,es,ar
# IPSTACK_API_KEY=

# --- Advanced Security & Compliance ---
# ENCRYPTION_KEY=           # 32-byte (e.g. base64)
# CORS_ALLOWED_ORIGINS=     # Comma-separated; overrides if set
# CLOUDFLARE_WEBHOOK_SECRET=

# --- Styling & Business Tools ---
# CLOUDINARY_URL=
# VAT_LAYER_API_KEY=
# PDF_GENERATOR_API_KEY=    # Prefer service with digital signatures (DocuSign/HelloSign) for contracts
# INTERCOM_APP_ID=
# UNSPLASH_ACCESS_KEY=
