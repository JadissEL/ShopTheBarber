/**
 * Disabled — account provisioning is handled server-side via POST /api/auth/provision.
 * Legacy auto-bootstrap created provider workspaces for clients with stale localStorage intent.
 */
export default function ProviderSignupBootstrap() {
  return null;
}
